apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ kustom.name }}
subjects:
- kind: ServiceAccount
  name: {{ kustom.name }}
roleRef:
  kind: Role
  name: {{ kustom.name }}
  apiGroup: rbac.authorization.k8s.io
